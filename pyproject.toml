[project]
name = "princeton-math-calendar"
version = "0.1.0"
description = "Princeton math calendar with speakers in event titles"
requires-python = ">=3.12"
dependencies = ["icalendar>=6.3,<8"]

[tool.uv]
package = false
